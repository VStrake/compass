# Anthropic API key used by the Edge Functions in /api
# Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional: Vercel KV credentials (for sharing recap URLs across devices).
# Wired up in Phase 9. Leave blank for v1.
KV_REST_API_URL=
KV_REST_API_TOKEN=
