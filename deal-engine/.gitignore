node_modules/
*.log
.DS_Store
.env
.env.local

# Real deal data is private and never committed. The tracked schema example
# is data/deal-stack.example.json; the live stack stays local and in OneDrive.
data/deal-stack.json

# Runtime outputs. These are generated per run and should not be committed.
archive/
exports/
sweep/questions.json
sweep/summary.md
